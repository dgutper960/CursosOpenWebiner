package cursos.javadesdecero.bucles;

public class BucleFor {
    public static void main(String[] args) {
        /** Cuando sabemos el número de iteraciones que necesitamos se usa for */
        //TODO SINTAXIS PARA FOR
        // for(estado_inical variable; condición_para_sentencia; incremento/decremento){
        // ... .... .... }

        /** Vemos un ejemplo **/
        /** Declaramos la variable para la iteración dentro del for */
        for(int contador = 0; contador <= 14; contador += 2){
            System.out.println(contador);
        } /****/
    }
}
